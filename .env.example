# Copy to .env and fill in values
# cp .env.example .env

# dev.to API key (required for article stats)
DEVTO_API_KEY=

# Optional: protect backend API with a shared secret
# Frontend must set NEXT_PUBLIC_DASHBOARD_API_KEY to the same value
DASHBOARD_API_KEY=
